export const REFRESH_TOKEN_SERVER_EMAIL = String(
  process.env.REFRESH_TOKEN_SERVER_EMAIL,
);

export const CLIENT_SECRET_SERVER_EMAIL = String(
  process.env.CLIENT_SECRET_SERVER_EMAIL,
);

export const CLIENT_ID_SERVER_EMAIL = String(
  process.env.CLIENT_ID_SERVER_EMAIL,
);

export const USER_CLIENT_SERVER_EMAIL = String(
  process.env.USER_CLIENT_SERVER_EMAIL,
);
